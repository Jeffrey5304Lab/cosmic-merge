# 正式發佈專用環境變數（由 `vite build --mode release` 載入，見 package.json 的 build:release）。
# 開啟這個旗標後，AdMob 會改用 src/config.ts 裡的正式廣告單元 ID（真實收益、點到會被稽核）。
# 平常的 `npm run dev` / `npm run build` / `npm run cap:ios` 都不會載入這個檔 → 一律走測試廣告。
VITE_ADMOB_LIVE=1
